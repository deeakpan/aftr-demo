/** Display face for Zedkr brand words (Orbitron loaded in app/layout.tsx). */
const brandFace = "font-brand";

export const brandPageTitle = `${brandFace} font-semibold tracking-wide text-[var(--foreground)]`;

export const brandSectionLabel = `${brandFace} text-xs font-semibold uppercase tracking-[0.14em] text-[var(--muted)]`;

/** Orbitron section title above cards — foreground, not accent. */
export const brandSectionHeading = `${brandFace} text-lg font-semibold tracking-wide text-[var(--foreground)] md:text-xl`;

export const brandSectionSubheading = "text-sm text-[var(--muted)]";

export const brandWord = `${brandFace} font-semibold tracking-wide text-[var(--foreground)]`;
